# IETF Filing Artifacts

Submission set for draft-sweeney-wimse-credential-delegation-00.

- `.mkd` is the kramdown-rfc source of truth. Regenerate the other files with `kdrfc -3`.
- `.xml` is the xml2rfc file that gets uploaded to https://datatracker.ietf.org/submit/
- `.txt` is the rendered text preview.
